# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

preset:
  name: outlook
  description: "Microsoft Outlook and Graph API access"

network_policies:
  outlook_graph:
    name: outlook_graph
    endpoints:
      - host: graph.microsoft.com
        port: 443
        protocol: rest
        enforcement: enforce
        tls: terminate
        rules:
          - allow: { method: GET, path: "/**" }
            exfil_risk: "GET exposes email, calendar, and contact data via Microsoft Graph"
          - allow: { method: POST, path: "/**" }
            exfil_risk: "POST can send email and create calendar events via Microsoft Graph"
      - host: login.microsoftonline.com
        port: 443
        protocol: rest
        enforcement: enforce
        tls: terminate
        rules:
          - allow: { method: GET, path: "/**" }
          - allow: { method: POST, path: "/**" }
      - host: outlook.office365.com
        port: 443
        protocol: rest
        enforcement: enforce
        tls: terminate
        rules:
          - allow: { method: GET, path: "/**" }
            exfil_risk: "GET exposes email content from Outlook"
          - allow: { method: POST, path: "/**" }
            exfil_risk: "POST can send email to any recipient via Outlook"
      - host: outlook.office.com
        port: 443
        protocol: rest
        enforcement: enforce
        tls: terminate
        rules:
          - allow: { method: GET, path: "/**" }
            exfil_risk: "GET exposes email content from Outlook"
          - allow: { method: POST, path: "/**" }
            exfil_risk: "POST can send email to any recipient via Outlook"
    binaries:
      - { path: /usr/local/bin/node }
